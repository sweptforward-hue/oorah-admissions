# Database Setup Guide

## Applying Canonical Migrations
Run the SQL scripts in sequence using the Supabase SQL Editor or Supabase CLI:

```bash
supabase db push
# or run the 4 canonical migration files in supabase/migrations/
```
